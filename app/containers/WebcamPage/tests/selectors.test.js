// import { fromJS } from 'immutable';
// import { makeSelectWebcamPageDomain } from '../selectors';

// const selector = makeSelectWebcamPageDomain();

describe('makeSelectWebcamPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
