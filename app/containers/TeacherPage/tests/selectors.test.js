// import { fromJS } from 'immutable';
// import { makeSelectTeacherPageDomain } from '../selectors';

// const selector = makeSelectTeacherPageDomain();

describe('makeSelectTeacherPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
